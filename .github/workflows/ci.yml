# @generated by ci-gen, do not edit

on:
    - push
    - pull_request
name: CI
jobs:
    linux-stable:
        name: linux stable
        runs-on: ubuntu-latest
        steps:
            - name: Checkout sources
              uses: actions/checkout@v2
            - name: Install toolchain
              uses: actions-rs/toolchain@v1
              with:
                  profile: minimal
                  toolchain: stable
                  override: true
            - name: cargo test
              uses: actions-rs/cargo@v1
              with:
                  command: test
                  args: --all --all-targets
            - name: cargo test --doc
              uses: actions-rs/cargo@v1
              with:
                  command: test
                  args: --doc
            - name: doc
              uses: actions-rs/cargo@v1
              with:
                  command: doc
    macos-stable:
        name: macos stable
        runs-on: macos-latest
        steps:
            - name: Checkout sources
              uses: actions/checkout@v2
            - name: Install toolchain
              uses: actions-rs/toolchain@v1
              with:
                  profile: minimal
                  toolchain: stable
                  override: true
            - name: cargo test
              uses: actions-rs/cargo@v1
              with:
                  command: test
                  args: --all --all-targets
            - name: cargo test --doc
              uses: actions-rs/cargo@v1
              with:
                  command: test
                  args: --doc
            - name: doc
              uses: actions-rs/cargo@v1
              with:
                  command: doc
    windows-stable:
        name: windows stable
        runs-on: windows-latest
        env:
            VCPKGRS_DYNAMIC: 1
        steps:
            - name: Checkout sources
              uses: actions/checkout@v2
            - name: Install toolchain
              uses: actions-rs/toolchain@v1
              with:
                  profile: minimal
                  toolchain: stable
                  override: true
            - name: cargo build only; no test
              uses: actions-rs/cargo@v1
              with:
                  command: build
    linux-beta:
        name: linux beta
        runs-on: ubuntu-latest
        steps:
            - name: Checkout sources
              uses: actions/checkout@v2
            - name: Install toolchain
              uses: actions-rs/toolchain@v1
              with:
                  profile: minimal
                  toolchain: beta
                  override: true
            - name: cargo test
              uses: actions-rs/cargo@v1
              with:
                  command: test
                  args: --all --all-targets
            - name: cargo test --doc
              uses: actions-rs/cargo@v1
              with:
                  command: test
                  args: --doc
            - name: doc
              uses: actions-rs/cargo@v1
              with:
                  command: doc
    linux-nightly:
        name: linux nightly
        runs-on: ubuntu-latest
        steps:
            - name: Checkout sources
              uses: actions/checkout@v2
            - name: Install toolchain
              uses: actions-rs/toolchain@v1
              with:
                  profile: minimal
                  toolchain: nightly
                  override: true
            - name: cargo test
              uses: actions-rs/cargo@v1
              with:
                  command: test
                  args: --all --all-targets
            - name: cargo test --doc
              uses: actions-rs/cargo@v1
              with:
                  command: test
                  args: --doc
            - name: doc
              uses: actions-rs/cargo@v1
              with:
                  command: doc
